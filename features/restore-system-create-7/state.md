# State: restore-system-create-7

| ID | Description | Wave | Status | Notes |
|----|-------------|------|--------|-------|
| T-001 | Delete C5 `NameProbe` (source + unit tests) | 1 | complete | f214bae. Filesystem-absence verified; Xcode test run deferred to Wave 2 (project intentionally non-compiling until T-004 rewrites `CreateDocumentHandler` references). |
| T-002 | Delete C6 `CreateTargetResolver` (source + unit tests, incl. writability probe) | 1 | complete | 31c0867. Same Wave-1 compile-break note as T-001. |
| T-003 | Delete C7 `LocalDocumentsFallback` (source + unit tests) | 1 | complete | a877a15. Same Wave-1 compile-break note as T-001. |
| T-004 | Reduce C4 to template-only delegate on `BrowserHostController`; strip deferred-write from `BrowserHostController` / `MarkdownDocumentSaveBridge`; remove `SceneDelegate.createHandler` | 2 | complete | b4a71dd. All 8 `CreateLocationTests` + `RemovedComponentsTests` Wave-2 tags pass. Deviations recorded in build-deviations.md D-1 (LaunchResumeBranch inline) and D-2 (spec-test API adaptation). |
| T-005 | Content-based initial-mode rule in `DocumentView.onAppear` (empty → raw + keyboard); strip create-path `initialMode` threading | 2 | complete | b4a71dd. All 6 `InitialModeTests` + 4 `ResumeRegressionTests` Wave-2 tags pass. `initialMode` and `focusEditorOnAppear` parameters removed from `DocumentView.init` (no remaining callers). |
| T-006 | Final sweep: delete `Markus_v3/Create/` + Xcode group; mirror `SystemCreateUITests`; prune obsolete `ResumeAndCreateUITests` create-flow assertions; full suite green | 3 | complete | (pending commit). `Markus_v3/Create/` already absent post-Wave-2 (D-4). Story B + `testBackFromUntypedNewFileLeavesNoFile` pruned from `ResumeAndCreateUITests.swift` (D-6). `SystemCreateUITests.swift` mirrored into Markus_v3UITests with 5 XCTSkip + 2 live tests (D-3). `ExternalChangeUITests.testNormalCreateTypeSaveProducesNoConflictSurfaces` rewritten to open an existing seeded file (D-7) since the new system-create flow breaks the "+→raw editor with focus" assumption. Markus_v3Tests suite: 243/243 pass. `RestoreSystemCreate7_*` (22 tests) all pass. UI tests pass in isolation; the 3 ExternalChangeUITests background/move tests that flake in full-suite runs (testBackgroundingDoesNotAutoResolveSheet / testBufferPreservedAcrossBackgroundWithPendingSheet / testDeleteThenReappearWithinWindowIsMoveNoBanner) are pre-existing environment-flake on this host (XCUITest `IDELaunchParametersSnapshot` / `Application failed preflight checks`), unrelated to this feature; they pass when re-run isolated. |
