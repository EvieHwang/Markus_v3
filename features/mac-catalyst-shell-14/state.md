# State — mac-catalyst-shell-14

Tracks build progress. Initialized by `/dag`; updated by `/next` as tasks complete.

Valid status values: `pending`, `in-progress`, `complete`, `failed`, `deviation`

| ID | Description | Wave | Status | Notes |
|----|-------------|------|--------|-------|
| T-001 | Enable the Mac (Catalyst) destination on the target so the project builds and launches as a Catalyst Mac app (target/config change; no new framework or deploy path); inherited iOS/iPad behavior unchanged | 1 | complete | 1e4edcd — SUPPORTS_MACCATALYST=YES on all three targets (Debug + Release); xcodebuild lists the Mac Catalyst destination; build succeeds. |
| T-002 | Catalyst menu bar (Component A) — File/Edit/View structure, ⌘-equivalents matching firing bindings, responder-chain enablement, each Markus item a trigger onto the existing EditorActions/presentDocument flow; no New | 2 | complete | b4e8114 — verification commit on top of WIP draft 3d9693c. CatalystMenuBuilder (File: Open…/Save/Close; View: Toggle Preview; Edit defers to system responder); AppDelegate.buildMenu override; BrowserHostController.handleFileOpenMenu(_:) selector for File → Open; EditorKeyCommandHostingController.{handleSave, handleClose, handleTogglePreview} reached via Selector("…") name-keying (the generic-class mangling risk flagged in the WIP is a non-issue — @objc methods on a generic class register under their declared name). `keyCommands` array gated `#if !targetEnvironment(macCatalyst)` so the menu's UIKeyCommands own the chords exclusively on Mac (HIG; no double-fire). **Verified:** 12/15 T-002 probes observed cleanly green across runs (the remaining 3 are pure-data assertions caught mid-execution by a reproducible iOS Simulator stack-guard overflow on the Swift cooperative pool — environmental, unrelated to T-002; D-002 in build-deviations.md). Mac Catalyst build succeeds and was launched live; all ten Catalyst contracts confirmed by the build owner against the running app: File/Edit/View menu structure with correct ⌘ equivalents and no New; stable-titled Toggle Preview on ⌘P; Save/Close/Toggle Preview grayed at the browser and enabled in the editor; File → Open always enabled; menu invocations dispatch through the same EditorActions / presentDocument seams as the chord bindings (no double-fire); ⌘O presents the system open panel from the browser; ⌘S/⌘W/⌘P at the browser are structural no-ops. **Deferred (D-002):** mirroring the T-002 subset of MacMenuBarUITests into Markus_v3UITests/ — requires a `-UITest-SeedFixture` launch-arg handler and fixture-seeding path that don't exist yet (likely co-introduced with T-006's restoration tests). |
| T-003 | File → Open adapter (Component B base) — system open panel constrained to MarkdownDocument.readableContentTypes funneling the chosen URL into the existing presentDocument(at:); cancel/fail/non-md/resume-target inherited; no second open path | 2 | complete | b3ae9f1 — MacOpenCommand adapter (UIDocumentPickerViewController, asCopy:false) + BrowserHostController.presentSystemOpenPanel()/openFileFromMenu(at:) routing through presentDocument(at:). 8 unit tests pass. |
| T-004 | Open-while-open composed host operation (Component B, F-001) — load-success-gated load → conditional teardown → present; failed new open leaves the prior document fully intact (DC-10); single window | 3 | complete | ec31a05 — BrowserHostController.openFileFromMenu(at:) gates on activeDocument: no prior → existing presentDocument(at:); prior → loadMarkdownDocument FIRST, then on .alert surface openPathAlert and return (DC-10), on .document call replaceActiveDocument(_:at:) which synchronously saves + stops the prior detector, clears session state, and presents the new editor in the dismiss completion (present sequenced after dismissal). 5 T-004 unit tests pass; 8 T-003 unit tests still pass; build succeeds. |
| T-005 | Pointer / hover affordance layer (Component C) — hover feedback on the tap-to-edit surface and the eye control; clickable == tappable; click == tap; never the sole affordance; inert with no pointer | 2 | complete | 5bb6771 — RenderedView tap-to-edit surface and DocumentView toolbar eye control each get SwiftUI `.hoverEffect(.automatic)` layered over the existing tap region (S-5; clickable == tappable; click == tap; FM-5 hit area unchanged). Eye button gains accessibilityIdentifier "ModeSwitchEyeButton" so the XCUITest mirror can locate it. 6 T-005 probe tests pass on iPhone 17 sim; 6 T-005 MacPointerUITests pass on Mac Catalyst (My Mac). The XCUITest mirror runs against the existing `-uitest-seed-last-file` resume fixture (the spec's `-UITest-SeedFixture mac-fixture.md` handler is still deferred — D-003) and taps/hovers at normalized center coordinates so XCUIElement.tap() lands on actual content rather than the wide-window padding outside the ~700pt centered content column. Two pre-existing iOS-only `XCUIDevice.shared.press(.home)` calls in `ExternalChangeUITests.swift` were gated `#if !targetEnvironment(macCatalyst)` so the UI test target compiles under Catalyst (matches T-002's keyCommands gate). See build-deviations.md → D-003 for the full set of T-005 build adaptations. |
| T-006 | Mac scene-restoration bridge (Component D) — relaunch defers document choice solely to LaunchResumeBranch/LastFileStore; single window; moved/deleted/first-launch fail closed to the browser with no error UI; no new identity store | 2 | complete | 9f65975 — `MacRestorationBridge` (Caseless enum, S-6 / FM-3 invariants surfaced as the `documentChoiceSource` / `persistsDocumentIdentity` static facts) calls through to the existing `LaunchResumeBranch.resume(into:store:)`; `SceneDelegate.scene(_:willConnectTo:options:)` routes the Mac Catalyst initialResumeAction through the bridge (iOS/iPad still call LaunchResumeBranch inline). 8/8 T-006 Swift Testing probe cases pass on iPhone 17 sim; 3/3 T-006 XCUITest cases pass on Mac Catalyst (`test_relaunch_restoresPreviouslyOpenDocument`, `test_firstLaunch_landsOnBrowser`, `test_movedOrDeletedPriorDocument_landsOnBrowserNoError`). **D-002 resolved in the same commit**: introduced the `-UITest-SeedFixture <name>` / `-UITest-ResetResumeStore` / `-UITest-ResumeFileUnreachable` launch-arg handlers in `LaunchResumeBranch.applyTestOverrides` (named-fixture seeder + reset/unreachable aliases) and used them to backfill the 9-case T-002-deferred subset of `MacMenuBarUITests` into `Markus_v3UITests/MacCatalystShell14_MacMenuBarUITests.swift` — 12/12 XCUITests (9 T-002 menu + 3 T-006 restoration) pass on Mac Catalyst. See build-deviations.md D-002 for the resolution note. The Catalyst destination renders `UIDocumentBrowserViewController` as the macOS Open dialog (window id `open-panel`); the browser-visible helper detects that marker first. Probe-level coverage in `Markus_v3Tests/MacCatalystShell14_RestorationTests.swift`. |
| T-007 | Mac app-icon slots (Component E) — populate the 12 empty idiom:mac slots in AppIcon.appiconset additively; iOS/iPad icon unregressed | 2 | deviation | 3ba3223 — D-001 (build-deviations.md): catalog actually declares 10 Mac slots (5 sizes × 2 scales), not 12 — corrected the count assertion. All 10 slots populated with sips-resized PNGs; iOS slots untouched. Tests pass. |
